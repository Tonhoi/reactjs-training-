import React, { Component} from 'react';

const Mounting = () => {
    
    return (
        <div>
            <div>components đã được mount vào DOM</div>
        </div>
    );
};

export default Mounting;